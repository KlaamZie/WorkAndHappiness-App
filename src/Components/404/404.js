export default function NoMatchPage(props) {
    // Un composant simple qui redirige vers la page d'accueil.
    // Ce composant est appeler si la route n'est associé à aucun composant "erreur 404"
    return window.location = "/"
}